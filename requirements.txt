# Runtime dependencies. Install with: pip install -r requirements.txt
# (For development/tests, also install: pytest httpx  -- see requirements-dev.txt)
fastapi>=0.111
python-multipart>=0.0.9
uvicorn>=0.30
chromadb>=0.5
openai>=1.40
pypdf>=4.2
pydantic-settings>=2.3
python-dotenv>=1.0
# Conversation state (WS7): Redis hot layer + an in-process fakeredis fallback
# so the no-install clone-and-run path works with no Redis server. SQLite (the
# durable tier) is stdlib.
redis>=5.0
fakeredis>=2.21
